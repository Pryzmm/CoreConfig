plugins {
    id "net.fabricmc.fabric-loom" version '1.15-SNAPSHOT' apply false
    id 'maven-publish'
}

allprojects {
    group = 'com.github.Pryzmm.CoreConfig'
}

tasks.withType(PublishToMavenLocal).configureEach { enabled = false }